(ns youtube-uploader.core-test
    (:require
    [pjstadig.humane-test-output]
    [cljs.test :refer-macros [is are deftest testing use-fixtures]]
    [youtube-uploader.core]))

(deftest test-core
  (is (= true true)))


